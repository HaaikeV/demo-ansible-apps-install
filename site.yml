- name: Connect and Install Application
  hosts: all
  gather_facts: true
  vars:
   ansible_connection: local
  tasks:
  - name: httpd package is present
    yum:
      name: httpd
      state: latest
    become: true